/*Requires jQuery*/

var accessPoint = 'HackinRequestReceiver';

function bindLocation(locationDOM, locationStr)
{

	locationDOM.html(locationStr);
}

function bindQuestion(htmlStr, quesDOM)
{
	quesDOM.html(htmlStr);
}